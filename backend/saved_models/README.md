Place your exported files from the notebook here:

- model.joblib
- artifacts.joblib

If these files are not present, the backend will still run using a heuristic fallback so you can build and test the UI.
